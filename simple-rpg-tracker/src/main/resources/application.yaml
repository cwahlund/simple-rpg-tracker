spring:
   datasource:
      username: simple_rpg_tracker
      password: simple_rpg_tracker
      url: jdbc:mysql://localhost:3306/simple_rpg_tracker
   
   jpa:
      hibernate:
         ddl-auto: none 
      show-sql: true
      
   sql:
      init:
         mode: always
         